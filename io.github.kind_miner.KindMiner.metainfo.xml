<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.kind_miner.KindMiner</id>

  <metadata_license>CC0-1.0</metadata_license>
  <project_license>MIT</project_license>

  <name>kind-miner</name>
  <summary>Idle Monero CPU mining for always-on machines</summary>

  <description>
    <p>
      kind-miner turns the idle cycles of an always-on desktop into Monero
      hashrate. There is no account, no pool operator, and no fee — payouts
      arrive directly in your own wallet via P2Pool's coinbase output.
    </p>
    <p>
      It stays out of your way: every few seconds it measures CPU usage
      (excluding its own threads) and throttles or suspends mining the moment
      you start doing real work, resuming automatically when you stop. Mining
      traffic is routed over Tor by default, so the node operator never sees
      your IP.
    </p>
    <p>Features:</p>
    <ul>
      <li>Disappears under load via a four-gear scheduler (Full, Reduced, Minimal, Paused)</li>
      <li>Configurable temperature ceiling and battery awareness</li>
      <li>Tor-native: routes through Tor by default</li>
      <li>Zero setup: XMRig and P2Pool are downloaded and SHA256-verified on first run</li>
      <li>Graphical window that tucks into the system tray, or a terminal-only mode</li>
    </ul>
  </description>

  <launchable type="desktop-id">io.github.kind_miner.KindMiner.desktop</launchable>

  <url type="homepage">https://github.com/kind-miner/kind-miner</url>
  <url type="bugtracker">https://github.com/kind-miner/kind-miner/issues</url>

  <developer id="io.github.kind_miner">
    <name>dMartian</name>
  </developer>

  <!--
    Screenshots are fetched at Flathub build time, so these raw URLs must
    resolve on the default branch. Regenerate the images from the repo root
    (absolute path — go test runs in the package directory):
      KM_SHOT_DIR="$PWD/assets/screenshots" go test -mod=mod \
        ./internal/gui -run TestCaptureScreenshots
  -->
  <screenshots>
    <screenshot type="default">
      <image>https://raw.githubusercontent.com/kind-miner/kind-miner/main/assets/screenshots/dashboard.png</image>
      <caption>Watch the miner step aside the moment you need your machine</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/kind-miner/kind-miner/main/assets/screenshots/onboarding-wallet.png</image>
      <caption>Setup starts with one question: where your rewards go</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/kind-miner/kind-miner/main/assets/screenshots/onboarding-kindness.png</image>
      <caption>Choose how much of the machine mining may take</caption>
    </screenshot>
  </screenshots>

  <!-- No adult/violent/gambling content. Mining is not gambling. -->
  <content_rating type="oars-1.1" />

  <releases>
    <release version="0.1.0" date="2026-05-27"/>
  </releases>
</component>
